class Solution {
public:
    bool rotateString(string s, string goal) {
        string st = s + s;
        if (s.length() != goal.length())
            return false;

        if (st.find(goal) != string::npos)
            return true;

        return false;
    }
};
